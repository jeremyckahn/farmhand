--- a/src/components/BailOutErrorBoundary/BailOutErrorBoundary.test.tsx
+++ b/src/components/BailOutErrorBoundary/BailOutErrorBoundary.test.tsx
@@ -16,10 +16,14 @@
   expect(screen.getByText('Child component')).toBeInTheDocument()
 })

-beforeEach(() => {
-  // JSDOM outputs uncaught error directly to console when error boundaries are hit.
-  vitest.spyOn(window, 'addEventListener').mockImplementation(() => {});
-})
+// Suppress JSDOM uncaught error logs for intentionally thrown errors in the test boundary
+const originalReportException = window.addEventListener;
+beforeAll(() => {
+  window.addEventListener('error', (e) => { e.preventDefault(); });
+});
+afterAll(() => {
+  window.addEventListener = originalReportException;
+});

 test('renders nothing when there is an error', () => {
   // Suppress console.error for this test
